{
  "name": "betsy-backend",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "mesh dev",
    "build": "mesh build && esbuild app.ts --bundle --sourcemap --platform=node --target=es2020 --outfile=dist/app.mjs",
    "test": "mocha tests/unit/",
    "lint": "eslint '*.ts' --quiet --fix",
    "compile": "tsc"
    
  },
  "dependencies": {
    "@graphql-mesh/cli": "^0.87.2",
    "@graphql-mesh/mysql": "^0.95.2",
    "@graphql-mesh/transform-filter-schema": "^0.95.2",
    "axios": ">=0.21.1",
    "graphql": "^16.7.1",
    "tslib": "^2.6.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "aws-cdk-lib": "^2.94.0",
    "chai": "^4.3.6",
    "esbuild": "^0.19.2",
    "mocha": "^10.1.0"
  }
}